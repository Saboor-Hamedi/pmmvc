# Delete the directory
rm -rf /var/www/html/mariadb/data
# run the docker 
docker compose up -d 
# build container 
docker compose up  --build
# docker name 
school-mvc-school-mvc-1
#folder structure
project-root/
├── app/
│   └── core/
│       └── Assets.php
└── public/
    ├── index.php
    └── assets/
        ├── css/
        │   └── style.css
        ├── js/
        │   └── script.js
        └── images/
            └── logo.png


# mvc notes 
if you want to make another folder controllers, you can add it by adding the folder names on 
$this->view('folder/filename');

# load image
# <link rel="stylesheet" href="<?php  Assets::assets('assets/css/style.css');?>">

# push tables into docker 
docker exec -i school-mvc-db-1 mariadb -u admin -psaboor123 school < tables.sql
# if you face this Warning: World-writable config file '/etc/mysql/conf.d/config.cnf' is ignored
# Adjust File Permissions:
docker exec -it school-mvc-db-1 bash
# Navigate to Configuration Directory:
cd /etc/mysql/conf.d/
ls -l
# Adjust Permissions:
chmod 644 config.cnf
exit
# Auth class
The auth method sets session variables when a user is authenticated.
The isAuthenticated method checks if a user is authenticated (logged in).
The getUser method retrieves user data if the user is authenticated.